use its_and_closures;



fn main() {
    let simulated_user_specified_value = 35;
    let simulated_random_number = 5;

    its_and_closures::generate_workout(simulated_user_specified_value, simulated_random_number);
}
